# Настройка GitHub репозитория

## Шаг 1: Создание репозитория на GitHub

1. Откройте браузер и перейдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или зарегистрируйтесь, если аккаунта нет)
3. Нажмите на кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму создания репозитория:
   - **Repository name**: `nest-server` (или любое другое название)
   - **Description**: `NestJS REST API with Prisma, PostgreSQL, Redis, OAuth, Swagger`
   - **Public/Private**: Выберите тип репозитория
   - ⚠️ **НЕ устанавливайте** флажки:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - (мы уже создали коммит с этими файлами)
5. Нажмите **"Create repository"**

## Шаг 2: Связывание с локальным репозиторием

После создания репозитория GitHub покажет инструкции. Используйте команды ниже:

### Вариант 1: HTTPS (рекомендуется для начинающих)

```powershell
cd d:\nest_server\server_nestjs

# Добавить удаленный репозиторий
git remote add origin https://github.com/ваш-username/название-репозитория.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Загрузить код на GitHub
git push -u origin main
```

### Вариант 2: SSH (требует настройки SSH ключей)

```powershell
cd d:\nest_server\server_nestjs

# Добавить удаленный репозиторий
git remote add origin git@github.com:ваш-username/название-репозитория.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Загрузить код на GitHub
git push -u origin main
```

## Шаг 3: Проверка

После выполнения команд:

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. Проверьте, что коммит отобразился: "Initial commit: NestJS project with Swagger refactoring and Heroku deployment config"

## Возможные проблемы

### Проблема 1: "Support for password authentication was removed"

Если при push через HTTPS получаете ошибку аутентификации:

**Решение**: Используйте Personal Access Token вместо пароля

1. Перейдите: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token (classic)"
3. Выберите scopes: `repo` (полный доступ)
4. Скопируйте сгенерированный токен
5. При запросе пароля вставьте токен вместо пароля

### Проблема 2: "fatal: remote origin already exists"

```powershell
# Удалить существующий remote
git remote remove origin

# Добавить заново
git remote add origin https://github.com/ваш-username/название-репозитория.git
```

### Проблема 3: Ошибка SSH ключей

Если используете SSH и получаете ошибку аутентификации:

1. **Проверьте наличие SSH ключа**:
   ```powershell
   cat ~/.ssh/id_rsa.pub
   ```

2. **Если ключа нет, создайте**:
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

3. **Скопируйте публичный ключ**:
   ```powershell
   cat ~/.ssh/id_ed25519.pub | clip
   ```

4. **Добавьте на GitHub**:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте скопированный ключ

## Следующие шаги после загрузки на GitHub

### 1. Настройка защиты веток (опционально)

GitHub → Repository → Settings → Branches → Add rule:
- Branch name pattern: `main`
- ✅ Require pull request reviews before merging
- ✅ Require status checks to pass before merging

### 2. Добавление секретов для CI/CD (для будущего)

GitHub → Repository → Settings → Secrets and variables → Actions:
- `DATABASE_URL`
- `REDIS_URL`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- и другие переменные окружения

### 3. Создание README с бейджами (опционально)

Добавьте в начало README.md:

```markdown
# NestJS Server

![Node.js](https://img.shields.io/badge/node-%3E%3D22.0.0-brightgreen)
![TypeScript](https://img.shields.io/badge/typescript-5.7.3-blue)
![NestJS](https://img.shields.io/badge/nestjs-11.0.1-red)
![Prisma](https://img.shields.io/badge/prisma-6.18.0-blueviolet)
![License](https://img.shields.io/badge/license-MIT-green)
```

## Полезные команды Git

```powershell
# Проверить статус
git status

# Проверить удаленные репозитории
git remote -v

# Создать новую ветку для разработки
git checkout -b develop

# Посмотреть историю коммитов
git log --oneline

# Отправить новую ветку на GitHub
git push -u origin develop

# Обновить локальную ветку из GitHub
git pull origin main
```

## Рабочий процесс для будущих изменений

```powershell
# 1. Внесите изменения в код
# 2. Проверьте что изменилось
git status
git diff

# 3. Добавьте изменения
git add .

# 4. Создайте коммит с описанием
git commit -m "feat: добавил новый функционал"

# 5. Загрузите на GitHub
git push origin main
```

## Форматы сообщений коммитов (Conventional Commits)

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

Примеры:
```
feat: добавил авторизацию через Facebook
fix: исправил ошибку с загрузкой файлов
docs: обновил README с инструкциями по деплою
refactor: вынес Swagger декораторы в отдельные файлы
```

## Готово! 🎉

После загрузки проекта на GitHub вы сможете:
- ✅ Деплоить на Heroku напрямую из GitHub (автоматические деплои)
- ✅ Настроить CI/CD с GitHub Actions
- ✅ Делиться кодом с другими разработчиками
- ✅ Отслеживать историю изменений
- ✅ Создавать Pull Requests для code review

---

**Следующий шаг**: После загрузки на GitHub следуйте инструкциям в `HEROKU_DEPLOY.md` для деплоя на Heroku
