# Swagger API Documentation

## 🚀 Доступ к документации

После запуска сервера, откройте в браузере:
```
http://localhost:3000/api/docs
```

## 📖 Что такое Swagger?

Swagger (OpenAPI) - это интерактивная документация API, которая позволяет:
- 📝 Просматривать все доступные эндпоинты
- 🧪 Тестировать API прямо из браузера
- 📤 Загружать файлы через веб-интерфейс
- 📊 Видеть структуру запросов и ответов
- 🔐 Авторизоваться и работать с защищенными эндпоинтами

## 🔑 Авторизация в Swagger

### Способ 1: Через Cookie (рекомендуется)
1. Сначала выполните вход через эндпоинт `/auth/login`
2. Cookie с сессией установится автоматически
3. Все последующие запросы будут авторизованы

### Способ 2: Ручная установка Cookie
1. Нажмите на кнопку "Authorize" 🔒 в правом верхнем углу
2. Введите значение cookie сессии
3. Нажмите "Authorize"

## 📁 Загрузка файлов через Swagger

### Пример: Создание инструмента с изображениями

1. Откройте эндпоинт `POST /tools`
2. Нажмите "Try it out"
3. Заполните поля:
   - `name`: Фреза концевая
   - `tool_id`: T001
   - `description`: Концевая фреза 12мм
   - `size`: 12mm
   - `type`: Фреза
4. В поле `files` нажмите "Choose Files" и выберите изображения
5. Нажмите "Execute"

## 🏷️ Теги (разделы) документации

- **Tools** - Управление инструментами
- **Machine CNC** - Управление станками
- **Work Overnight** - Управление работами
- **Files** - Управление файлами
- **Auth** - Аутентификация

## 📋 Примеры использования

### 1. Вход в систему
```
POST /auth/login
{
  "email": "admin@example.com",
  "password": "password123"
}
```

### 2. Создание инструмента
```
POST /tools
Content-Type: multipart/form-data

name: Фреза
tool_id: T001
files: [image1.jpg, image2.jpg]
```

### 3. Получение всех инструментов
```
GET /tools
```

### 4. Обновление инструмента
```
PATCH /tools/1
Content-Type: multipart/form-data

description: Обновленное описание
files: [new-image.jpg]
```

## 🎨 Цветовая схема статусов

- 🟢 **2xx** - Успешный запрос
- 🔵 **3xx** - Перенаправление
- 🟡 **4xx** - Ошибка клиента (неверные данные, не авторизован)
- 🔴 **5xx** - Ошибка сервера

## 🔒 Коды ответов

### Успешные ответы
- `200 OK` - Запрос выполнен успешно
- `201 Created` - Ресурс создан

### Ошибки клиента
- `400 Bad Request` - Неверные данные
- `401 Unauthorized` - Не авторизован
- `403 Forbidden` - Недостаточно прав
- `404 Not Found` - Ресурс не найден

### Ошибки сервера
- `500 Internal Server Error` - Внутренняя ошибка сервера

## 💡 Советы

1. **Используйте фильтры** - В Swagger можно фильтровать эндпоинты по тегам
2. **Копируйте curl** - Каждый запрос можно скопировать как команду curl
3. **Изучайте схемы** - Раскрывайте секции "Schema" для детального просмотра структуры данных
4. **Проверяйте примеры** - В документации есть примеры для каждого поля

## 🛠️ Разработка

При добавлении новых эндпоинтов используйте декораторы:

```typescript
import { ApiTags, ApiOperation, ApiResponse } from '@nestjs/swagger'

@ApiTags('Tools')
@Controller('tools')
export class ToolsController {
  
  @ApiOperation({ summary: 'Создание инструмента' })
  @ApiResponse({ status: 201, description: 'Успешно создан' })
  @Post()
  create() {
    // ...
  }
}
```

## 📞 Поддержка

Если документация не загружается:
1. Убедитесь, что сервер запущен
2. Проверьте порт (по умолчанию 3000)
3. Очистите кеш браузера
