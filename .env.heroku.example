# Пример переменных окружения для Heroku
# Скопируйте эти переменные и замените значения на свои

# Application
NODE_ENV=production
APPLICATION_PORT=${PORT}  # Heroku автоматически установит PORT

# Database (автоматически создается Heroku)
POSTGRES_URI=${DATABASE_URL}  # Heroku предоставит DATABASE_URL

# Redis (автоматически создается Heroku)
REDIS_URI=${REDIS_URL}  # Heroku предоставит REDIS_URL

# Cookie & Session
COOKIE_SECRET=your-strong-cookie-secret-min-32-chars-here
SESSION_SECRET=your-strong-session-secret-min-32-chars-here
SESSION_NAME=session_id
SESSION_DOMAIN=.herokuapp.com
SESSION_MAX_AGE=7d
SESSION_HTTP_ONLY=true
SESSION_SECURE=true
SESSION_FOLDER=sessions:

# CORS
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-app-name.herokuapp.com

# OAuth - Google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-app-name.herokuapp.com/auth/oauth/callback/google

# OAuth - GitHub
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=https://your-app-name.herokuapp.com/auth/oauth/callback/github

# Email (Resend)
EMAIL_HOST=https://api.resend.com
EMAIL_FROM=noreply@yourdomain.com
RESEND_API_KEY=re_your_resend_api_key

# ===== Инструкция по установке переменных =====
#
# Используйте команды ниже, чтобы установить переменные в Heroku:
#
# heroku config:set NODE_ENV=production
# heroku config:set APPLICATION_PORT=\$PORT
# heroku config:set POSTGRES_URI=\$(heroku config:get DATABASE_URL)
# heroku config:set REDIS_URI=\$(heroku config:get REDIS_URL)
# heroku config:set COOKIE_SECRET="ваш-секрет"
# heroku config:set SESSION_SECRET="ваш-секрет"
# ... и так далее для каждой переменной
#
# Или установите все сразу из файла .env:
# cat .env.heroku | xargs heroku config:set
